This repo holds the test cases for the testApp.
Also, there is a python file that resets the database.
In the resources/seleniumide directory, one can use those files
with Firefox's Selenium IDE to run in the browser.
